<div class="w-full">
  <% if current_user.admin %>
    <div class="flex justify-between items-center">
      <h1 class="font-bold text-4xl">Ads</h1>
      <%= link_to 'New sale', new_ad_path, class: "rounded-lg py-3 px-5 bg-blue-600 text-white block font-medium" %>
    </div>
  <% end %>

  <%= render 'search_form' %>

  <%= turbo_frame_tag "ads" do %>
    <%= render 'ads/index' %>
  <% end %>

</div>